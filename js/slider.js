import{throttle}from"./utils.js";const slider=document.querySelector(".slider"),sliderInner=slider.querySelector(".slider__inner"),sliderInput=slider.querySelector(".slider__control-input"),beforeImage=slider.querySelector(".slider__before"),afterImage=slider.querySelector(".slider__after");function initSlider(){if(null===slider)return null;const e=throttle((()=>setSlider(sliderInput)),500);setSlider(sliderInput),sliderInput.addEventListener("input",(e=>setSlider(e.target))),window.addEventListener("resize",e)}function setSlider(e){const r=e.value/100*sliderInner.clientWidth,t=(100-e.value)/100*sliderInner.clientWidth,i=100*Number(sliderInput.value)/(sliderInput.max-sliderInput.min);beforeImage.style.width=`${r}px`,afterImage.style.width=`${t}px`,sliderInner.style.setProperty("--slider-progress",i.toString())}export{initSlider};